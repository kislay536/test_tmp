# Auto-generated by Metro-MPI++
# DO NOT EDIT MANUALLY

# Usage: python3 view_json.py | column -t 

import json

with open("partition_report.json") as f:
    data = json.load(f)

keys = [
    "partition",
    "port_name",
    "direction",
    "width",
    "active",
    "type",
    "Comm",
    "mpi_process",
    "mpi_rank",
]
print("	".join(keys))

for partition_name, rows in data["partitions"].items():
    for r in rows:
        print("	".join([
            partition_name,
            str(r.get("port_name", "")),
            str(r.get("direction", "")),
            str(r.get("width", "")),
            # str(r.get("active", "")),
            ("🟢" if str(r.get("active", "")).lower() == "yes" else "🔴"),
            str(r.get("type", "")),
            str(r.get("Comm", "")),
            str(r.get("mpi_process", "")),
            str(r.get("mpi_rank", "")),
        ]))

